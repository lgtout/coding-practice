package com.lagostout.elementsofprogramminginterviews.graphs

object PaintBooleanMatrix {

    enum class Color {
        BLACK, WHITE;
    }

    data class PointColor(val point: Point, val color: Color)

    fun flipRegionColor(grid: List<List<PointColor>>, start: Point) {

    }

}
